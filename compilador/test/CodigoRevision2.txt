//Mi primer Comentario - Versión Completa (TESTING LOOPS STEP BY STEP)

// ============================================================
// DECLARACIÓN DE VARIABLES GLOBALES (FUERA DE PROCEDIMIENTOS)
// ============================================================
Haz variable1 5
Haz variable2 "Hola"
Haz variable3 TRUE
Haz mivar 0
Haz var 0
Haz var3 0
Haz numero 0
Haz salida 0
Haz var1 0
Haz bucle 1
Haz resBool FALSE

// ============================================================
// PROCEDIMIENTO PRINCIPAL (VERSIÓN PARCIAL - SOLO HAZ.HASTA)
// ============================================================
Para principal []
[
  // ✅ BLOQUE 1: Variables básicas (FUNCIONA)
  INIC mivar = 100;
  INIC mivar = 100 + 45;

  // ✅ BLOQUE 2: Comandos de Tortuga (FUNCIONA)
  avanza 10
  av 15

  retrocede 10
  re 15

  giraderecha 90
  gd 45

  giraizquierda 90
  gi 45

  // ✅ BLOQUE 3: Visibilidad y Posición (FUNCIONA)
  ponpos [100 0]
  ponxy 0 0

  ponrumbo 90

  ponx 100
  pony 100

  // ✅ BLOQUE 4: Control de Lápiz (FUNCIONA)
  bajalapiz
  subelapiz

  // ✅ BLOQUE 5: Colores RGB (FUNCIONA)
  poncl [0 0 255]
  poncolorlapiz [0 0 255]

  // ✅ BLOQUE 6: Colores por Nombre (FUNCIONA)
  poncl azul
  poncolorlapiz rojo

  // ✅ BLOQUE 7: Centro y Espera (FUNCIONA)
  centro
  espera 10

  // ✅ BLOQUE 8: Ejecuta (FUNCIONA)
  ejecuta [av 10 gd 90]

  // ✅ BLOQUE 9: Repite (FUNCIONA)
  repite 4 [avanza 10 giraderecha 90]

  // ✅ BLOQUE 10: SI simple (FUNCIONA)
  SI (10>2) [re 10 gd 20]

  // ✅ BLOQUE 11: SI con ELSE (FUNCIONA)
  SI (var1==1)
  [re 10 gd 20]
  [re 25 gd 10]

  // ✅ BLOQUE 12: HAZ.HASTA (FUNCIONA CORRECTAMENTE)
  INIC numero = 0;
  INIC salida = 0;
  HAZ.[
    INIC numero = numero + 1;
  ] HASTA (numero==3)

  // ❌ BLOQUE 13: HAZ.MIENTRAS (DESHABILITADO - LOOP INFINITO)
  // INIC bucle = 1;
  // HAZ.[
  //   INIC numero = numero + 1;
  //   INIC bucle = 0;
  // ] MIENTRAS (bucle==1)

  // ❌ BLOQUE 14: MIENTRAS (DESHABILITADO - LOOP INFINITO)
  // INIC var = 3;
  // MIENTRAS (var==3) [
  //   INIC salida = 0;
  //   INIC var1 = 0;
  //   INIC var = 0;
  // ]

  // ✅ BLOQUE 15: Funciones Booleanas (TESTING)
  INIC resBool = iguales?(10, 2*5);
  INIC resBool = Y(10>2, 2>5);
  INIC resBool = O(10>2, 2>5);
  INIC resBool = mayorque?(10, 2*5);
  INIC resBool = menorque?(10, 2*5);
]
fin

// ============================================================
// LLAMADA AL PROCEDIMIENTO PRINCIPAL
// ============================================================
centro
bajalapiz
principal[]
subelapiz

